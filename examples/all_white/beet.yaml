

resource_pack:
  load: .


pipeline:
  - model_resolver.plugins.render_all_context

output: ../../dist

meta:
  model_resolver:
    # load vanilla item models
    minecraft_version: "26.1-snapshot-7"
    use_cache: true