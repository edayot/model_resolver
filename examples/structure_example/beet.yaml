

resource_pack:
  load: .

data_pack:
  load: .

pipeline:
  - my_plugin

output: ../../dist

meta:
  model_resolver:
    # load vanilla item models
    load_vanilla: false
    render_size: 1024
    minecraft_version: "26.1-pre-1"
    special_rendering: true