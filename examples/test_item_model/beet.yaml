

resource_pack:
  load: .

pipeline:
  - my_pipeline

output: ../../dist


meta:
  model_resolver:
    # load vanilla item models
    load_vanilla: false
    minecraft_version: "1.21.4"
    use_cache: true
    special_rendering: true